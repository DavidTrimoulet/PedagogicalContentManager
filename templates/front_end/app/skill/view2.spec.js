'use strict';

describe('myApp.skill module', function() {

  beforeEach(module('myApp.skill'));

  describe('skill controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var skillCtrl = $controller('SkillCtrl');
      expect(skillCtrl).toBeDefined();
    }));

  });
});