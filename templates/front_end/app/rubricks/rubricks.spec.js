'use strict';

describe('myApp.rubricks module', function() {

  beforeEach(module('myApp.rubricks'));

  describe('rubricks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rubricksCtrl = $controller('RubricksCtrl');
      expect(rubricksCtrl).toBeDefined();
    }));

  });
});