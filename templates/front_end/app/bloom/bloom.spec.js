'use strict';

describe('myApp.bloom module', function() {

  beforeEach(module('myApp.bloom'));

  describe('bloom controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bloomCtrl = $controller('BloomCtrl');
      expect(bloomCtrl).toBeDefined();
    }));

  });
});