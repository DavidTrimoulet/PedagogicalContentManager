'use strict';

describe('myApp.problem module', function() {

  beforeEach(module('myApp.problem'));

  describe('problem controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var problemCtrl = $controller('ProblemCtrl');
      expect(problemCtrl).toBeDefined();
    }));

  });
});